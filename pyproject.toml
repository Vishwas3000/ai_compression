[project]
name = "jpeg-vs-jpeg-ai"
version = "0.1.0"
description = "Reproducible JPEG vs JPEG AI rate-distortion benchmarks"
requires-python = ">=3.10"
dependencies = ["Pillow>=10"]

[tool.uv]
package = false
